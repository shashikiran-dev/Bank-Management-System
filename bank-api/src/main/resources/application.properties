spring.application.name=bank

spring.datasource.url=jdbc:mysql://localhost:3306/bank_db
spring.datasource.username=root
spring.datasource.password=shashi14

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.flyway.enabled=false

# Server port
server.port=8082

# ── Admin credentials ──────────────────────────────────────────────────
bank.admin.username=admin
bank.admin.password=SecureAdmin@123

# ── Email (Gmail SMTP) ─────────────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shashikuruva2004@gmail.com
spring.mail.password=hwehlhtljjfwjlft
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8

# ── Banking Rules ──────────────────────────────────────────────────────
# Minimum balance that must remain after any withdrawal or transfer
bank.rules.savings-min-balance=500.00
bank.rules.current-min-balance=0.00

# Per-transaction caps
bank.rules.max-withdrawal-per-tx=200000.00
bank.rules.max-transfer-per-tx=200000.00
bank.rules.max-deposit-per-tx=1000000.00

# Daily cumulative limits (resets at midnight)
bank.rules.daily-withdrawal-limit=50000.00
bank.rules.daily-transfer-limit=100000.00

# JWT Configuration
bank.jwt.secret=SecureBankSuperSecretKey2024ksk!!
bank.jwt.expiry-hours=2
